export type SiteConfig = typeof siteConfig;

export const siteConfig = {
	name: "NextJS | NextUI boilerplate",
	description: "Este proyecto boilerplate de NextJS y NextUI",
	navItems: [
		
	],
	navMenuItems: [
		
	],
	links: {
		
	},
};
